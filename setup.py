from setuptools import setup, find_packages

setup(
    name="factChecker",
    version="0.1.0",
    packages=find_packages(),
    author="Pushkar Goyal",
    description="A Fact Checker application with MCP server.",
    python_requires='>=3.8',
)